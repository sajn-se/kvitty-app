# Better Auth secret key - Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=

# Better Auth URL - Your application URL
BETTER_AUTH_URL=http://localhost:3000

# Database connection string
DATABASE_URL=

# AWS S3 Configuration
AWS_REGION=eu-north-1
AWS_S3_BUCKET=kvitty-se
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=
CLOUDFRONT_DOMAIN=

# Email provider: 'console' (default, logs to console), 'smtp', or 'usesend'
EMAIL_PROVIDER=console

# Email sender address
EMAIL_FROM=

# SMTP Configuration (required when EMAIL_PROVIDER=smtp)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
SMTP_SECURE=false

# Usesend Configuration (required when EMAIL_PROVIDER=usesend)
USESEND_API_KEY=
USESEND_URL=

# Public application URL
NEXT_PUBLIC_APP_URL=http://localhost:3000

# Groq API key for AI features
GROQ_API_KEY=

# Encryption key for sensitive data - Generate with: openssl rand -base64 32
ENCRYPTION_KEY=

# Google OAuth client ID
GOOGLE_CLIENT_ID=

# Google OAuth client secret
GOOGLE_CLIENT_SECRET=
